# MEMORY — major decisions

Log: **What / Why / Rejected**. Agents append brief entries after non-trivial decisions.

---

- **2026-07-03** — Art renames/uploads use Firebase `shared/artConfig` + `shared/imageLibrary` (open rules for testing only). jsonblob remains fallback for stats.
- **2026-07-03** — Docs under `.docs/` (not `docs/`). Stack is CRA `react-scripts` 5, not Vite.
