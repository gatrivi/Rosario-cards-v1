# Handoff — 2026-08-14

## Shipped

- Santa Faustina / optional prayer sheet now fills the viewport on desktop and keeps the prayer variant tabs inside the modal surface.
- Optional prayers now route through bundled Fish audio only; browser TTS is no longer the implicit fallback.
- Build passes after the layout change.

## Notes

- Existing dirty files under `scripts/` and older booklet changes were left untouched.
