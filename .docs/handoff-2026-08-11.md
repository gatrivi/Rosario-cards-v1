# Handoff — 2026-08-11 (v0.3.79)

## Recording Studio

- `RecordingStudioView` now exposes the full booklet devotion roster via `BOOKLET_MYSTERY_IDS`.
- Review progress is persisted in `localStorage` per language + devotion.
- Saving a Tier S clip marks that step as reviewed.
- Added `Sin revisar` / `Revisados` filters for progressive cleanup.

## Booklet devotion audit

- Canonical booklet ids live in `src/utils/bookletSequence.js`.
- `BookletView` reaches the full roster through the main mystery pills plus the shelf entries.
- The Via Crucis / Via Lucis modes are reachable through `StationsDevotionThumb`.
- No missing booklet devotion ids were found in the active UI paths.

## Dirty / leave alone

- `scripts/fish-*-missing.json` (modified, not committed)
- `scripts/fish-en-bake.log`, `scripts/prayer-image-usage.json` (local dumps)
- `src/components/Views/AssetStudio.jsx`, `src/components/Views/BookletView.jsx`, `src/components/Views/RecordingStudioView.jsx`, `src/components/Views/DevotionImageReviewPanel.jsx` are part of the current working set.
